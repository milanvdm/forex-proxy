package forex.http.rates

import org.scalatest.{ FlatSpec, Matchers }

class RatesHttpRoutesSpec extends FlatSpec with Matchers {

  "routes" should "correctly decode from and to" in pending

  it should "correctly encode the rate response" in pending

}
